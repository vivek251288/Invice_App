server:
  port: 9199

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vivek
    username: postgres
    password: Vivek
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
